<nav class="app-header navbar navbar-expand bg-body">
    <div class="container-fluid">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link d-flex align-items-center" data-lte-toggle="sidebar" href="#" role="button">
                    <i class="bi bi-list fs-3"></i>
                </a>
            </li>
        </ul>

        <ul class="navbar-nav ms-auto">
            <li class="nav-item">
                <a class="nav-link" href="#" data-lte-toggle="fullscreen">
                    <i data-lte-icon="maximize" class="bi bi-arrows-fullscreen"></i>
                    <i data-lte-icon="minimize" class="bi bi-fullscreen-exit" style="display: none"></i>
                </a>
            </li>

            <li class="nav-item dropdown user-menu">
                <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown">
                    <img
                            src="{{ asset('build/avatar5.png') }}"
                            class="user-image rounded-circle shadow"
                            alt="User Image"
                    />
                    <span class="d-none d-md-inline">{{ app.user.usernameCanonical }}</span>
                </a>
                <ul class="dropdown-menu dropdown-menu-lg dropdown-menu-end mt-2" style="margin-right: -10px;">
                    <li class="user-header text-bg-dark bg-opacity-75">
                        <img
                                src="{{ asset('build/avatar5.png') }}"
                                class="rounded-circle shadow"
                                alt="User Image"
                        />
                        <p>{{ app.user.usernameCanonical }}</p>
                    </li>

                    <li class="user-footer">
                        <a href="#" class="btn btn-light btn-flat">Perfil</a>
                        <a href="{{ path('nucleos_user_security_logout') }}" class="btn btn-light float-end">Salir</a>
                    </li>
                </ul>
            </li>
        </ul>
    </div>
</nav>
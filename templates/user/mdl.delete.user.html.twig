<div class="modal fade" id="mdl-delete-user" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1"
     aria-labelledby="staticBackdropLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="box text-decoration-none m-0 border-0">
                <div class="modal-header">
                    <h5 class="modal-title">Confirmaci&oacute;n</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <p class="modal-title fs-5 pb-3">
                        ¿Est&aacute; seguro de eliminar el usuario?
                    </p>
                    <table class="table table-bordered table-striped table-hover table-sm m-0">
                        <tr>
                            <th>Usuario</th>
                            <td ng-bind="ctrl.username"></td>
                        </tr>
                        <tr>
                            <th>Email</th>
                            <td ng-bind="ctrl.email"></td>
                        </tr>
                        <tr>
                            <th>Nombre</th>
                            <td ng-bind="ctrl.nameUser"></td>
                        </tr>
                    </table>
                </div>
                <div class="modal-footer justify-content-between">
                    <button type="button" class="btn btn-secondary btn-sm" data-bs-dismiss="modal">Cancelar</button>
                    <button type="button" class="btn btn-primary btn-sm" ng-click="ctrl.deleteUser()">Eliminar</button>
                </div>
                <div class="overlay" ng-if="ctrl.isLoadingModalDeleteUser">
                    <i class="fa fa-spin fa-spinner"></i>
                </div>
            </div>
        </div>
    </div>
</div>